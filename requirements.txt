# --- analytics engine (shared by Streamlit app, CLI, and the web API) ---
pandas>=2.0
numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
networkx>=3.2

# --- decoupled web API + static frontend (api.py) ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# --- original Streamlit front end (app.py); not needed for the web/API build ---
streamlit>=1.37

# Optional (better lemmatization). The app runs fine without these — it falls
# back to a corpus-checked rule lemmatizer. To enable spaCy:
#   pip install spacy
#   python -m spacy download en_core_web_sm
# spacy>=3.7
